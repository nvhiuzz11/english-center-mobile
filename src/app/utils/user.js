export const isParent = role => {
  return role === 'parent';
};

export const isStudent = role => {
  return role === 'student';
};

export const isTeacher = role => {
  return role === 'teacher';
};
